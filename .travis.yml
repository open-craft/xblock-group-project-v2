language: python
python:
    - "2.7"
before_install:
    - "export DISPLAY=:99"
    - "sh -e /etc/init.d/xvfb start"
install:
    - "npm install intern"
script:
    - node_modules/.bin/intern-runner config=tests/js/intern_config
notifications:
  email: false
addons:
  firefox: "36.0"
