<activitystage
    id='{{ activity_stage.id }}' name='{{ activity_stage.name }}' type='{{ activity_stage.type }}'
    {% ifnotequal activity_stage.open_date None %} open='{{ activity_stage.open_date }}'{% endifnotequal %}
    {% ifnotequal activity_stage.close_date None %} open='{{ activity_stage.close_date }}'{% endifnotequal %}
>
  {% ifnotequal activity_stage.content_html None %}
    <content>
    {% autoescape off %}
      {{ activity_stage.content_html }}
    {% endautoescape %}
    </content>
  {% endifnotequal %}
  {% for question in activity_stage.questions %}
    <question id='{{question.id}}'>
      <label>
        {{ question.label.text }}
      </label>
      <answer>
        {% autoescape off %}
          {{ question.answer_html }}
        {% endautoescape %}
      </answer>
    </question>
  {% endfor %}
  {% for assessment in activity_stage.assessments %}
    <assessment id='{{assessment.id}}'>
      <label>
        {{ assessment.label.text }}
      </label>
      <answer>
        {% autoescape off %}
          {{ assessment.answer_html }}
        {% endautoescape %}
      </answer>
    </assessment>
  {% endfor %}
  <resources>
    {% for document in documents %}
      <document title='{{ document.title }}'{% ifnotequal document.grading_criteria None %} grading_criteria='true'{% endifnotequal %}{% ifnotequal document.description None %} description='{{ document.description }}'{% endifnotequal %}>
        {{ document.location }}
      </document>
    {% endfor %}
  </resources>
  <submissions>
    {% for document in group_activity.submissions %}
      <document {% ifnotequal document.description None %}description='{{ document.description }}' {% endifnotequal %}title='{{ document.title }}'></document>
    {% endfor %}
  </submissions>
</activitystage>
